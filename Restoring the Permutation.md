# 極値制約から順列を復元する一般テクニック

出典：Codeforces「Permutation Cuts」の解法を一般化したもの。

このテクニックは、**「区間の最大値／最小値の系列が与えられ、それを満たす順列（または数列）の個数を数える」**タイプの問題全般に応用できる。

---

## 1. 適用条件（このテクニックが刺さる問題の特徴）

以下のシグナルが揃っていたら疑う：

- 順列 $p_1,\dots,p_n$ に対して、**区間最大値（または最小値）** に関する条件が複数個与えられている
  - 例：「位置 $i$ で切ったときの左右の最大値の min/max」
  - 例：「$\max(p_1,\dots,p_i)$ の列が与えられる」
  - 例：「累積 max/min の列が与えられる」
- 求めるものが「条件を満たす順列の**個数**」（modで答える）
- $n$ が大きい（$10^5\sim10^6$）ので $O(n)$ または $O(n\log n)$ が要求される

このタイプは一見「全探索や DP が必要そう」に見えるが、実は**最大値（または最小値）の位置を軸にした構造的な一意分解**が可能なことが多い。

---

## 2. 解法の3段階テンプレート

### Step 1：極値（ $n$ や $1$ ）の位置を軸に構造を固定する

順列の中で**最大値 $n$（または最小値 $1$）がどこにあるか**に着目する。

- $n$ を含む側の区間最大値は**常に $n$**になる、という事実を使うと、
  「与えられた条件列」のうち「$n$ を含まない側」の条件だけが実質的な情報を持つことが分かる。
- これにより、条件列が「$n$ の位置を境に、ある種の**単調性**（非減少→非増加、など）を持つはず」という必要条件が導ける。

→ **一般化**：「全体の最大値／最小値がどちらの区間に属するか」で場合分けすると、片方の条件は自明になり、他方だけが実質的な制約になる。この非対称性が構造を生む。

### Step 2：候補となる「軸の位置」が定数個に絞られることを示す

素朴には軸（$n$の位置候補）は $O(n)$ 通りありうるように見えるが、多くの場合：

- 単調性が両方向から成立する範囲は「**値が一定のプラトー（同じ値の連続区間）**」に対応する
- プラトーの**内部**は、順列の一意性（同じ値が2箇所に出せない）などの制約で自動的に矛盾する
- 生き残るのは**プラトーの境界（値が変化する点）と全体の両端**のみ

→ **一般化**：「単調性が両側から要求される区間の交わり」は、条件が定数値になる「フラットな部分」になりがちで、その内部は別の制約（一意性、単射性など）で潰れることが多い。**候補が$O(n)$個に見えても、実際は定数個しか矛盾なく残らない**、というのは典型的な落とし穴＆典型的な救いの構造。困ったら「本当に全部有効か」「一意性制約と衝突しないか」を疑う。

### Step 3：値を昇順（または降順）に処理し、「強制」と「自由」を仕分けて数え上げる

軸を固定したら、残りの位置に値をどう割り当てられるかを数える。ここで使うのが本テクニックの核：

1. 与えられた条件列を、軸に近い順（＝値が小さい順になるように）並べる
2. 値を**小さい方から順に**処理する
3. 各値 $x$ について：
   - **初出現**（＝その側で「新しく記録を更新する」瞬間）→ 置く値は**一意に確定**（強制、倍率 $1$）
   - **既出**（＝記録を更新しない、単に「その値以下で埋めるだけ」の位置）→ **未使用かつ $x$ 未満の値なら何でもよい**
     - この時点で既に $used$ 個の値を使っているなら、選択肢は $x - used$ 通り
4. すべての位置についての倍率を掛け合わせると、その軸の位置に対応する順列の総数になる
5. 有効な軸候補（Step 2で絞ったもの）すべてについて合計する

→ **一般化**：
> 「ある位置に置かれる値が、それより手前の情報から一意に決まるか、それとも自由に選べるか」を、**値の小さい順に処理しながら仕分けていく**。自由に選べる場合の選択肢数は「**その値未満でまだ使われていない値の個数**」＝ $x - (\text{既に使った値の個数})$ という単純な式になる。

このパターンは、**順列を「小さい値から確定させていく」構成的カウント**の典型形であり、以下のような別問題群でも同型の発想が現れる：
- 転倒数・特定の統計量を満たす順列の数え上げ
- 各位置での「これまでの最大値／最小値」の系列（record系列）が与えられて元の順列を復元する問題
- スタック／キューでソート可能な順列の数え上げ（record更新のタイミングで選択肢が分岐する系）

---

## 3. 実装上の注意点（この問題での学び）

- **候補点の判定**：`pre[i]`（非減少フラグ）・`suf[i]`（非増加フラグ）のような**累積フラグ配列**を前計算しておくと、$O(1)$ で各候補をチェックできる。実装時に `pre[cut]` のように**必ず添字をつける**こと（`pre` だけだと配列＝ポインタの真偽評価になり、意図と異なる挙動になるバグの典型例）。
- **マージ処理**：左右2つの単調列をマージして1本の昇順列にする際、同じ値が同時に複数本の列から出てきた場合の扱い（＝矛盾なら答え0）を忘れずに処理する。
- **計算量**：候補点が定数個であることを最初に示せれば、各候補につき $O(n)$ のマージ処理をしても全体 $O(n)$（テストケースあたり）に収まる。「候補が定数個」の証明をサボると、この計算量見積もりの正当化ができない点に注意。

---

## 4. この技法を思い出すためのキーワード

問題文に以下のような言葉が出てきたら、この一般テクニックを想起する：

- 「区間の最大値／最小値」「累積max/min」「記録（record）」
- 「順列の個数を数えよ」＋「mod で出力」
- 「ある位置で分割したときの左右の統計量」
- 制約列が与えられていて、それを満たす元の配列・順列を復元／数え上げる

このとき考えるべき問い：
1. **全体の最大値（または最小値）はどちらの側にあるか？** → 片側の条件が自明になる非対称性を探す
2. **単調性の両立範囲は本当に全部有効か？** → プラトー内部が一意性制約と矛盾しないか確認し、候補を定数個に絞る
3. **値を昇順に処理したとき、各位置は「強制」か「自由」か？** → 自由なら「$x - used$ 通り」の形で数え上げる

## 連続した領域において内部に領域外が存在するのか
　グリッドの範囲を縦横に2つずつ広げる。そして(0, 0)から探索を始め、領域外の個数を達成できているなら内部に存在せず、達成できないなら存在する。

## 特定の区間の白黒を反転
　いくつかの図形が与えられ、その図形の範囲の白黒を反転させて最終的に全て白にするという操作を考える。図形の形を考えて、グリッドに適切に周期的な数字を割り当てると、同じ数字を持つマスの集合において不変量が現れることがある。特に(i + j)%mのような周期でグループ分けすることを考えればよい。

## グリッドの構成可能性
出典 : 2219/problem/A Grid L<br>
|とL字型の棒だけでn * mのグリッドが構成可能かを判定する。　<br>
claim : We can fill an m×n
 grid using p
 segments and q
 L's if and only if p+2q=m(n+1)+n(m+1)
 and p≥|m−n|
.
https://codeforces.com/blog/entry/152936
ここでは省略されているが重要な事実 : グリッドの総線分は2nm + n + m = S, 両辺を二倍して1を足すと、(2n + 1)(2m + 1) = 2S + 1。つまり2S + 1の約数ペアを探すとそのまま2m + 1, 2n + 1になる。

## グリッドの全マス移動
　出典 : 2217/problem/C Grid Covering<br>
　n * mのグリッドの(1, 1)からスタートして縦にa, 横にbずつ交互に進む。この時、グリッドの全マスをcoverできるか。<br>
 まず必須条件としてgcd(n, a) = 1 && gcd(m, b) = 1が必要である。これで次に(1, 1)に着くまで一切同じマスを通ることは無い。
 次に(1, 1)に着くまでの移動回数を考えると2 * lcm(n, m)である。2 * lcm(n, m) >= n * m <=> 2 >= gcd(n, m)が必要。
 以上の二つで十分であるから求める条件はgcd(n, a) = 1 && gcd(m, b) = 1 && 2 >= gcd(n, m)。

## 切り捨て除算された数に対してMODをとる方法
　a/b ≡ d (mod M)を満たすdを求めたいとき、aが直接計算できないほど大きいときaに対してmod M * Mを取り、それをMで除算するだけで求めることができる。
proof)　a/b = M * p + qとしたときa = M * (M * p + q) + dと表せる。a ≡ M * q + d (mod M * M)であるから、a/M = q。

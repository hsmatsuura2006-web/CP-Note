重複消去(文字列)-
　例えば文字列の中に文字を挿入することを考えるそのとき入れる場所によって重複が発生してしまう。そのとき元の文字列の文字を新しく作った文字列の一番左に対応させる。文字列の一番最後の位置ごとに対応させ方を考えるとよい。元の文字列の文字から次の文字列の文字に進むとき新しく入れる文字は次の文字列の文字以外でなくてはならない。このようにすることで新しい文字列の中の文字列の位置に関わらず、25.pow(元の文字列の最後の文字までの隙間の数)となる。abc171_f

重複消去(最小周期)-
　最小周期で一意に定まるのなら、それを上位の周期から除くことで約数DPに帰着できる。実質的にメビウス反転の場合が多いが、単純な減算でよいときもある。abc304_f
　
二次元平面上の格子パスへの帰着-
　例えば白と黒のボールが有限個あったとしてそれぞれを並び替えるという操作を行う。もし並び替えるときの制限があるなら、それをグリッド上で表現することで簡単に表せることもある。その時はパスの経路の数に帰着することができ、必要なら鏡像法を用いる。abc205_e

鏡像法-
　グリッド上で、スタートからゴールまでx >= y + kを満たすような経路を考える。
https://noshi91.hatenablog.com/entry/2021/07/10/170943#fn-e9ea19b8
詳細は上記のサイトを参照。NGな経路の集合と反転したスタート地点からのすべての経路の集合が全射になることを利用する。そもそもゴールにたどり着けない時は自動的に0にならないため注意。abc205_e

包除原理(全種類が最低一回現れる, subjection(全射)の数え上げ, 第二種スターリング数)-
　例えばある一定の種類を用いて数列を構成するときの数列の総数など。https://codeforces.com/gym/707498/problem/C
使わなければならない要素の中で、使われていない要素の数を増やしていき(-1)^i * nCi * (n - i) * kを足し合わせればよい。

全体集合から要素数が偶数もしくは奇数個の部分集合の数え上げ-
　偶数個の集合でも奇数個の場合でも、2^(n - 1)個。

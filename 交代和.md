交代和をsum := Σ(-1)^k * a[i]とする。また配列aを広義単調増加とする。aの部分配列の中で交代和がゼロとなるような個数を考える。この時aの同じ値のグループの中から同じ数を二つ選ぶとき打ち消しあうことが分かる。したがって交代和を計算するときは実質的に狭義単調増加の配列を考えればよい。狭義単調増加の交代和はゼロになることは無い。よってゼロになるためには同じ値のグループから同じ値を偶数個選ぶことは必要十分条件である。

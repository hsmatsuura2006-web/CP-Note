## 和からXORとANDへの変換
　a + b = a XOR b + 2(a AND b)

## 中点XOR補間列の周期性
長さ $2^k + 1$ の配列があり、両端 $a_1 = A$, $a_{2^k + 1} = B$ からスタートして、中点 $a_m = a_l \oplus a_r$ （ただし $m = \frac{l+r}{2}$）を埋めていく（Codeforces 2234D）。<br>
証明は難しいため省くが、周期 3 が現れます。これは、出現する値が結局 $A, B, A \oplus B$ の 3 種類のみであることからも予想できる。

## XOR Sort
　配列aが与えられ、i < jとなるようなインデックスを選んだ時 i bitwise XOR j <= kを満たすようなi, jならば $a_{i}$ と $a_{j}$ を交換する。
 
　
　
